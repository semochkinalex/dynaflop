import Authenticate from '../../components/authenticate/authenticate';
import styles from './auth.module.css';

const Auth = () => {

    return (
        <main>
          <Authenticate />
        </main>
    )
}

export default Auth;